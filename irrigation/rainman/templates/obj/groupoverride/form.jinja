{% haml %}

-extends "base.jinja"
-block title
	-if groupoverride is defined
		=groupoverride
		=" – edit"
	-else
		New Group Time

-block content
	%h1
		-if groupoverride is defined
			=groupoverride
		-else
			=self.title()
	%form action="" method="post"
		-csrf_token
		%table
			=form.as_table()|safe
		%input type="submit" value="Save"

{% endhaml %}
