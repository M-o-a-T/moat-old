{% haml %}

-extends "base.jinja"
-block title
	-if day is defined
		=day.name
		=" – edit"
	-else
		New time part

-block content
	%h1
		-if day is defined
			=day.name
		-else
			=self.title()
	%form action="" method="post"
		-csrf_token
		%table
			=form.as_table()|safe
		%input type="submit" value="Save"

{% endhaml %}
