{% haml %}

-extends "base.jinja"
-block title
	-if site is defined
		=site.name
	-else
		New site

-block content
	%h1
		=self.title()
	%form action="" method="post"
		-csrf_token
		%table
			=form.as_table()|safe
		%input type="submit" value="Save"

{% endhaml %}
