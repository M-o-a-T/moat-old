{% haml %}

-extends "base.jinja"
-block title
	-if envitem is defined
		=envitem
		=" – edit"
	-else
		New Environment item for {{envgroup}}

-block content
	%h1
		-if envitem is defined
			=envitem
		-else
			=self.title()
	%form action="" method="post"
		-csrf_token
		%table
			-if envitem is defined
				-set envgroup=envitem.group
			%tr
				%th -> Environment Table
				%td
					%a href="/envgroup/{{ envgroup.id }}"
						=envgroup.name
			=form.as_table()|safe
		%input type="submit" value="Save"

{% endhaml %}
