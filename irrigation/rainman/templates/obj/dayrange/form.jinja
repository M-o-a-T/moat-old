{% haml %}

-extends "base.jinja"
-block title
	-if dayrange is defined
		=dayrange.name
		=" – edit"
	-else
		New time spec

-block content
	%h1
		-if dayrange is defined
			=dayrange.name
		-else
			=self.title()
	%form action="" method="post"
		-csrf_token
		%table
			=form.as_table()|safe
		%input type="submit" value="Save"

{% endhaml %}
